[project]
name = "devmax-api"
version = "0.1.0"
description = "Unprompted backend: conversational spaced-repetition study coach"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlmodel>=0.0.22",
    "asyncpg>=0.30",
    "alembic>=1.14",
    "pydantic-settings>=2.6",
    "anthropic>=0.40",
    "aioapns>=3.2",
    "httpx>=0.28",
    "pyjwt[crypto]>=2.13",
    # Security floors for transitive crypto and APNs HTTP/2 dependencies.
    "cryptography>=50.0.0",
    "h2>=4.4.1",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "aiosqlite>=0.20",
    "ruff>=0.8",
    "greenlet>=3.5.4",
    "pip-audit>=2.9",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.flake8-bugbear]
# FastAPI's dependency-injection idiom is a call in the argument default.
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query"]
